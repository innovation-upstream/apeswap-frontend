import { ThemeUIStyleObject } from 'theme-ui'

export const styles: Record<string, ThemeUIStyleObject> = {
  playBody: {
    lineHeight: '14px',
    fontSize: '12px',
    fontWeight: 500,
    marginBottom: '10px',
  },
}
